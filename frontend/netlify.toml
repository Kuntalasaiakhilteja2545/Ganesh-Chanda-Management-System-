[build]
  base = "."
  publish = "dist"
  command = "npm run build"

[build.environment]
  VITE_API_BASE_URL = "https://ganesh-chanda-backend.railway.app/api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200